import {style} from "../../../../utils/styleUtils";

export const togglerStyles = style({
   toggler: {
      all: "unset", position: "relative",
      width: 20,
      height: 20,
      marginRight: 10,
      cursor: "pointer",
   }
})