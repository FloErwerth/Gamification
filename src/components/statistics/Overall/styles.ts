import {style} from "../../../utils/styleUtils";

export const styles = style({
   wrapper: {},
   filterButtons: {
      display: "grid",
      height: 50,
      gridTemplateColumns: "auto auto auto auto",
   }
})