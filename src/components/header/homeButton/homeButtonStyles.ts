export const homeButtonStyles = {
   wrapper: {
      all: "unset",
      cursor: "pointer",
      display: "flex",
      alignItems: "center",
   },
   title: {
      fontSize: 20,
   },
   icon: {
      width: 50,
      height: 50,
   }
} as const;