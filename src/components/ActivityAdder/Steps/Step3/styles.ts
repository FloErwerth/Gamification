import {style} from "../../../../utils/styleUtils";

export const styles = style({
    wrapper: {
        display: "flex",
        flexDirection: "column",
        gap: 20,
    },
    title: {
        fontWeight: "bold",
        fontSize: 16,
        marginBottom: 10,
    }
})