import {style} from "../../utils/styleUtils";

export const inlineModalOpenerStyles = style({
   openerButton: {
      all: "unset",
      ":hover": {
         textDecoration: "underline",
         cursor: "help",
      }
   }
})