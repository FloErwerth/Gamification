import {style} from "../../utils/styleUtils";

export const styles = style({
   statWrapper: {
      margin: 10,
      display: "flex",
      flexDirection: "row",
      alignItems: "center",
      fontSize: 18,
      gap: 5,
   },
   value: {
      fontSize: 18,
      fontWeight: "bold",
      fontStyle: "italic",
   }
})